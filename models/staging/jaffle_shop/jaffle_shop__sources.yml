
sources:
  - name: raw_jaffle_shop
    database: raw
    schema: jaffle_shop
    description: clone of a postgres application process
    # freshness:
    #       warn_after:
    #         count: 6
    #         period: hour
    #       error_after:
    #         count: 1000
    #         period: day
    # loaded_at_field: loaded_at_field

    tables:
      - name: customers
        description: raw cust data
        freshness: null     
        columns:
          - name: ID
            description: primary key for cust data
            tests:
              - unique 
              - not_null
      - name: orders
        columns:
          - name: ID
            tests:
              - unique 
              - not_null        